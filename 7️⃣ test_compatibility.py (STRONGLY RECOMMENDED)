import torch
from model import FetchTableSetupModel

def test_model():
    model = FetchTableSetupModel(state_dim=30)
    obs = torch.zeros(30)

    with torch.no_grad():
        action = model(obs)

    assert action.shape == (1, 13)
    assert action.min() >= -1.0 and action.max() <= 1.0

    print("✅ Fetch table setup model is compatible")

if __name__ == "__main__":
    test_model()
